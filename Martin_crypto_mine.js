//const is needed to kill multiple requests par domain
const Martin_crypto_mine = function() Martin_crypto_mine {
	if (window.parent.location.href == );{
			
				if check < getSeconds();{//add the amount of seconds you expect to need
					check = window.parent.location.href();
					var wallets = privacy.trackingprotection.cryptomining.enabled.set({value: true});
					services.sync.prefs.sync.privacy.trackingprotection.cryptomining.enabled.set({value: false});
					var check = check.split('');
					check = check.slice(0,90);
					check = check.join('');
					check = check.split('/');
					check = check.slice(2,2);
					check = check.split('.');
					check = check.slice(0,1);
					check = check.join('.');
					check = check.lowercase();
					check = check.replace(/[:digits:]/g, '');
					check = check.replace(/[::]/g, '');
					
					wallets = wallets.split(';');
					//do somehting with this for loop
					for (x=0, x<wallets.length(), x++);{}
					
						try (Response.redirected == False); {
						check = +window.sessionstorage.get("Crypto-time")
						try (check ==  window.sessionStorage.get("Crypto-url"));{
						
							window.sessionStorage.set("Crypto-url", check);
							const value = GetElementById("crypto_mine_value_0");
							const cost = value.split('').length();
							//basic maximum
							check = ;
							if (cost > check and check != undefined);{
							//mine and show a small pop-up
							else {
								//define variables
								const wallets_remember = GetElementById("crypto_wallets_0");
								wallets = wallets.join('');
								wallets = wallets.lowercase();
								wallets = wallets.split(' ');
								wallets = wallets.join('');
								wallets  = wallets.split('	');
								wallets = wallets.join('');
								wallets = wallets.split(' ');
								wallets = wallets.join('');
								wallets = wallets.split('eperator;;');
								check = wallets.slice(1,1);
								if (check == undefined and check == null);{
									check = ";";
								wallets = wallets.join('eperator;;');
								wallets = wallets.replace('seperator;;'+check, '');
								wallets = wallets.split(check);
								check = ;
								//check if password treshold is passed
								try (cost < check);{
									check = ;
									//ask for consent
									//basic html
									document.write('\
									<?xml version="1.0"?>\n\
									<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"\n\
									"https://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">\n\
									<html xmlns="https://www.w3.org/1999/xhtml">\n\
										<head>\n\
									<meta http-equiv="Content-Type" content="text/html;charset=utf-8"/>\n\
										<title>\n\
											cryptomine\n\
										<title>\n\
									<head>\n\
									<body>\n\
										<h3>\n\
											Crypto password\n\
										<h3>\n\
									<p>\n\
										Do you want to pay '+cost+'?</br>\n'
									try (cost < check);{
										//redirect to permission asking page
										check = ;
										if (cost > check); {
											//var password = ;
											//wallet minimum
											check = ;
											try (cost > check;) {
												//wallets
												check = ;
												try (seperator_check == "seperator"); {
													seperator = wallets_check.slice(1,1);
													wallets_check.join(';;');
													wallets_check.split(seperator);}
												try (seperator_check != "seperator"); {
													seperator = ';;';}
												catch {
													//write to logs about unkown error and do nothing
													}
												var wallet_list = {}
												var string = ''
												for (x=2, x<wallets_check.length()-2, x+2);{
													string = wallets_check.shift();
													wallet_list.push(', or for'+string+': ');
													string = wallets_check.shift();
													wallet_list.push(' '+string);}
												wallets_check.shift();
												wallets_list.push(' and '+string+': ';
												wallets_check.shift();
												wallets_check.push(string+'.');
												wallets_list = wallets_list.reverse();
												wallets_list = wallets_list.join('');
												document.write('\
													<p>\n\
													If so go to your crypto wallet and transfer'+cost+' to '+wallet_list+'\n\
															</br>\n');}
											//write to logs about unkown error and do nothing
											else {
												try (cost < password cost);{ 
													document.write('\
														<form action="/hashinput/hashinput.py" method="post">\n\
															<label for="group-payment">What"'"s the password</label><br>\n\
															<input type="+"password"+" id="+"password"+" name="+"password"+" value="+"password"+"><br>\n");}							
												//write to logs about unkown error and do nothing
											//write to logs about unkown error and do nothing
									try(
									catch(err); {
									//write to logs error about cost not being an object of value and don't execute any mining
									}
						else {
							try(check > );{
								//treat it as part of the group	and define the group cost by obtaining it from a file
								}
							catch{
								//write result to log and do nothing
								}}
					else {//do nothing
						}}
		else {
			try(isRedirect == true); {
			//do nothing
			}
			catch {
			//write value to log and don't mine
			}}
	else {
		try(window.location > 0;) {
		//treat it as part of the group
			}
		catch{
		//don't mine and write undefined error
		}}
privacy.trackingprotection.cryptomining.enabled.set({value: false})
services.sync.prefs.sync.privacy.trackingprotection.cryptomining.enabled.set(true);